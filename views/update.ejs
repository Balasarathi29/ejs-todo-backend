<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update page</title>
    <link rel="stylesheet" href="css/index.css">
</head>
<body>
    <div class="todo-container">
        <h2 class="title"><%= title %></h2>
        <button class="btn" onclick="window.location.href='/'">Back</button>

        <form action="/update-todo/<%= todo._id %>" method="post">
            <div class="form-block">
                <div class="form">
                    <label for="Title">Title</label>
                    <input type="text" name="title" placeholder="Enter the Title..." value="<%= todo.title %>" required>
                    <label for="Desc">Description</label>
                    <input type="text" name="desc" placeholder="Description" value="<%= todo.desc %>" required>
                </div>
                <button class="form-btn">Save</button>
            </div>

        </form>
    </div>
</body>
</html>